name: trip-update-tracking
entrypoint: trip_tracking.py:main_flow
work_pool: Main
work_queue: default
infra_overrides:
  env:
    EXTRA_PIP_PACKAGES: aiohttp google-transit pytz supabase pandas pydantic
deployments:
- name: trip-update-tracking
  version:
  tags: []
  concurrency_limit:
  description:
  entrypoint: trip_tracking.py:main_flow
  parameters: {}
  work_pool:
    name: Main
    work_queue_name:
    job_variables: {}
  enforce_parameter_schema: true
  schedules:
  - interval: 8.0
    anchor_date: '2024-09-26T22:37:26.306087+00:00'
    timezone: UTC
    active: true
    max_active_runs:
    catchup: false
  pull:
  - prefect.deployments.steps.git_clone:
      repository: https://github.com/kfalconarkn/gtfs-bus-tracking.git
      branch: main
      access_token: '{{ prefect.blocks.secret.deployment-trip-update-tracking-gtfs-bus-tracking-trip-update-process-repo-token
        }}'
